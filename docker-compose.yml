version: "3.6"

services:
  nginx:
    image: nginx:latest
    container_name: nginx
      #ports:
      #- 80:80
    network_mode: host
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped

